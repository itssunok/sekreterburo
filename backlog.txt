BACKLOG

Priority key: MUST = blocks a credible launch · SHOULD = do soon after launch
· COULD = nice to have, no urgency.

## MUST HAVE (launch blockers)

- [ ] Contact form backend is wired up (functions/api/quote.js, sends via
      Resend: office notification + visitor auto-reply, file uploads
      included) but not usable for real customers yet — Resend is still in
      sandbox mode, which can only deliver to the account's own test address
      (sun@sociablenerd.com), currently set as QUOTE_NOTIFY_EMAIL. To go
      live: verify sekreterburo.com as a sending domain at
      resend.com/domains (add the DNS records Resend generates at whichever
      registrar manages the domain's DNS — not Cloudflare), then once
      verified, update two Cloudflare Pages env vars: RESEND_FROM_EMAIL to
      "Sekreter Büro <info@sekreterburo.com>" and QUOTE_NOTIFY_EMAIL to
      info@sekreterburo.com. No code changes needed for that step.
- [ ] No favicon — browser tabs and bookmarks show a blank/default icon.

## SHOULD HAVE (do soon after launch)

- [ ] No Open Graph / Twitter Card meta tags — links shared on WhatsApp,
      LinkedIn, etc. won't show a title/description/image preview.
- [ ] LocalBusiness JSON-LD schema is live but has no `sameAs` (Google
      Business Profile / Facebook / Instagram / LinkedIn URLs) — need to
      check with Sekreter Büro whether these exist before adding them.

## COULD HAVE (nice to have)

- [ ] Custom domain sekreterburo.com is planned but not yet configured.
      Once it's live: point DNS at Cloudflare Pages and add the custom
      domain in the Cloudflare Pages project settings, then update the
      canonical/hreflang URLs in index.html and en/index.html, plus
      sitemap.xml and robots.txt, from https://itssunok.github.io/sekreterburo/
      (and its /en/ variant) to the real domain.
